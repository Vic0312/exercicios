//Exercício 11
using System;

namespace Exercicios{
    class Ex11{
        /*static void Main(string[] args){
            double qtdcomb, dt, consumo;
            Console.WriteLine("Digite em km a distância percorrida: ");
            dt = Convert.ToDouble(Console.ReadLine());

            Console.WriteLine("Digite em litros a quantidade gasta de combustivél: ");
            qtdcomb = Convert.ToDouble(Console.ReadLine());
            
            consumo = dt/qtdcomb;

            if(dt<=0 || qtdcomb<=0)
            {
                Console.WriteLine("Valor inválido");
            }
            else
            {
                if(consumo<8)
                {
                    Console.WriteLine("Venda o carro agora que ele bebe mais que opala!");
                }
                else
                {
                    if(consumo>=8 && consumo<=12)
                    {
                        Console.WriteLine("Pense em vender o carro!");
                    }
                    else
                    {
                        if(consumo>12)
                        {
                            Console.WriteLine("Relativamente econômico!");
                        }
                    }
                }
            }
            
        }*/
    }
}